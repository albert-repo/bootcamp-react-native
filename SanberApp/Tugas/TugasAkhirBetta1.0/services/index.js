const { TextComponent } = require("react-native");

export const baseProdUrl = 'https://temen.in';

export const loginApi = baseProdUrl + '/api/sappexpense/login';

export const logoutApi = baseProdUrl + '/api/sappexpense/logout';